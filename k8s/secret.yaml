# Secret for watsonx.data Demo Application
# Note: In production, use external secret management (e.g., Sealed Secrets, Vault)

apiVersion: v1
kind: Secret
metadata:
  name: wxdata-secret
  namespace: wxdata-demo
  labels:
    app: wxdata-demo
type: Opaque
stringData:
  # Base64 encode these values in production
  WATSONX_USERNAME: "ibmlhadmin"
  WATSONX_PASSWORD: "password"

# Made with Bob